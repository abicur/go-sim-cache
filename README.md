# go-sim-cache

This package provides a simple in-memory cache. It's use `sync.Map` as a data storage. 

## Installation

``go get github.com/abicur/go-sim-cache``


## Tests

To run tests use command:

``go test``

